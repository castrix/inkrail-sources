# Third-party notices

Original Inkrail code is MIT licensed. Dependencies and generated release bundles retain their upstream license terms. `package-lock.json` pins the dependency graph; installed dependency `package.json`, LICENSE, and NOTICE files are authoritative for each version.

Principal dependencies: Nuxt (MIT), Vue (MIT), Prisma (Apache-2.0), Playwright (Apache-2.0), Sharp (Apache-2.0), Zod (MIT), Cheerio (MIT), bcryptjs (BSD-3-Clause), and impit (Apache-2.0). Development dependencies also carry their own terms. Source packages bundle parser dependencies; retain esbuild's legal comments and provide the source repository and its lockfile with releases.

The reader optionally requests Inter, Literata, and Noto Serif TC through Google Fonts. Fonts are not embedded in this code archive. Consult upstream font license files when self-hosting those fonts. No downloaded novels, manga, browser profiles, or personal generated library artwork are distributed here.
